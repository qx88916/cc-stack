---
description: Icon and asset management rules for React Native / Expo apps
globs: apps/{passenger,driver}/**/*.{ts,tsx}
alwaysApply: false
---

# React Native Asset & Icon Rules

## SVG vs PNG — Know the Difference

This project uses `react-native-svg-transformer` in Metro config:

- **`.svg` files** → React components (rendered via `<SvgIcon />`)
- **`.png` files** → image assets (used with `<Image source={...} />`)

These are **NOT interchangeable**.

## When Adding New Icons

1. Check `constants/index.ts` — all icons are imported as `.png` and used via `<Image source={icons.xxx} />`
2. Always provide a `.png` file for any new icon in `assets/icons/`
3. Optionally add an `.svg` version if the icon will also be used as a component

```tsx
// ❌ WRONG — SVG imported as image asset will crash
import briefcase from "@/assets/icons/briefcase.svg";
<Image source={briefcase} />

// ✅ CORRECT — PNG imported as image asset
import briefcase from "@/assets/icons/briefcase.png";
<Image source={briefcase} tintColor="#000" />
```

## Icon Checklist (Before Committing)

- [ ] New icon has a `.png` file in `assets/icons/`
- [ ] Import in `constants/index.ts` uses `.png` extension
- [ ] Icon is exported in the `icons` object in `constants/index.ts`
- [ ] If replacing an emoji, use `<Image source={icons.xxx} />` not inline text

## Never Do

- Import `.svg` as `<Image source={}>` — fails at bundle time
- Add icons to `constants/index.ts` without the corresponding `.png` file
- Assume `.svg` and `.png` are interchangeable in React Native
