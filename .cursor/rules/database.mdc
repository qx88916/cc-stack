---
description: Database and data modeling standards
globs: apps/backend/src/{models,migrations,schemas}/**/*.{js,ts}
alwaysApply: false
---

# Database Standards

## Schema Design

```javascript
// ✅ GOOD - Proper indexes and constraints
const BookingSchema = new Schema({
  passengerId: { 
    type: ObjectId, 
    ref: 'User', 
    required: true,
    index: true 
  },
  pickupLocation: {
    type: { type: String, enum: ['Point'], required: true },
    coordinates: { type: [Number], required: true }
  },
  status: { 
    type: String, 
    enum: ['pending', 'confirmed', 'completed', 'cancelled'],
    default: 'pending',
    index: true
  },
  createdAt: { type: Date, default: Date.now, index: true }
});

// Add geospatial index for location queries
BookingSchema.index({ pickupLocation: '2dsphere' });
```

## Query Optimization

```javascript
// ❌ BAD - N+1 query problem
const bookings = await Booking.find();
for (const booking of bookings) {
  const driver = await Driver.findById(booking.driverId);
}

// ✅ GOOD - Use populate/join
const bookings = await Booking.find()
  .populate('driverId')
  .select('pickupLocation status createdAt')
  .limit(20);
```

## Validation

```javascript
// ✅ GOOD - Schema-level validation
const UserSchema = new Schema({
  phone: {
    type: String,
    required: true,
    unique: true,
    validate: {
      validator: (v) => /^\d{7}$/.test(v),
      message: 'Phone must be 7 digits'
    }
  }
});
```

## Required Practices

- Add indexes for frequently queried fields
- Use transactions for multi-document operations
- Implement soft deletes (deletedAt field)
- Store timestamps (createdAt, updatedAt)
- Validate data at schema level
- Use connection pooling
- Handle duplicate key errors gracefully
